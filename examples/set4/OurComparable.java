public interface OurComparable {

    /**
     * Returns negative if this is less than o.
     * Returns 0 if this is equal to o.
     * Returns positive number if this is greater than o.
     */
    int compareTo(OurComparable o);

}
